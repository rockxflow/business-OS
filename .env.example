# Rockxflow OS - Environment Configuration
# Copy to .env and set real values. NEVER commit .env with secrets.
FLASK_SECRET_KEY=change-me-to-a-long-random-string-min-32-chars
PORT=5000
# Optional: set a custom database path
# DB_PATH=data/rockxflow.db
